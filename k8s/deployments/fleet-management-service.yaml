apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-management-service
  namespace: connected-cars
  labels: { app: fleet-management-service, version: v1 }
spec:
  replicas: 2
  selector:
    matchLabels: { app: fleet-management-service }
  template:
    metadata:
      labels: { app: fleet-management-service, version: v1 }
    spec:
      serviceAccountName: fleet-management-service
      containers:
        - name: fleet-management-service
          image: ghcr.io/vuppalapatisn/connected-cars/fleet-management-service:1.0.0
          ports: [{ containerPort: 8084 }]
          envFrom:
            - configMapRef: { name: platform-config }
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom: { secretKeyRef: { name: platform-secrets, key: SPRING_DATASOURCE_URL } }
            - name: DB_USERNAME
              valueFrom: { secretKeyRef: { name: platform-secrets, key: DB_USERNAME } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: platform-secrets, key: DB_PASSWORD } }
          resources:
            requests: { cpu: "250m", memory: "384Mi" }
            limits:   { cpu: "1",    memory: "768Mi" }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8084 }
            initialDelaySeconds: 20
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8084 }
            initialDelaySeconds: 40
---
apiVersion: v1
kind: Service
metadata:
  name: fleet-management-service
  namespace: connected-cars
  labels: { app: fleet-management-service }
spec:
  selector: { app: fleet-management-service }
  ports:
    - name: http
      port: 8084
      targetPort: 8084
